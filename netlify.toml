# Configuración de despliegue para Netlify
[build]
  command = "npm run build"
  publish = "dist"

# La app es single-page: cualquier ruta sirve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
